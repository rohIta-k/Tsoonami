<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TSOONAMI | Login & Register</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:wght@100..700&family=Kirang+Haerang&family=Poppins:wght@100..900&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="/css/signIn.css">
</head>
<body>
    <img src="/assets/bg.jpeg" alt="Background" class="bg-image">
    <div class="message" id="auth-message"></div>

    <div class="login-popup">
        <div class="left-div">
            <div class="title">TSOONAMI</div>
            <img src="/assets/left-div.jpg" alt="Brand Image" class="left-img">
        </div>

        <div class="right-content">
            <div class="tabs">
                <button class="tab-button active" id="logbutton">Log in</button>
                <button class="tab-button" id="regbutton">Register</button>
            </div>

            <div id="login-container">
                <h2>Log in to your account</h2>
                <form class="auth-form" id="login-form">
                    <div class="input-group">
                        <input type="email" placeholder="Email" pattern="^[a-zA-Z0-9._%+\-]+@gmail\.com$" required id="login-email">
                    </div>
                    <div class="input-group">
                        <input type="password" placeholder="Password" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" required id="login-password">
                    </div>
                    <button type="submit" class="login-button">Log in</button>
                </form>
            </div>

            <div id="register-container" style="display: none;">
                <h2>Create account</h2>
                <form class="auth-form" id="register-form">
                    <div class="input-group">
                        <input type="text" placeholder="Name" pattern="^[A-Za-z\s]+$" required id="register-name">
                    </div>
                    <div class="input-group">
                        <input type="email" placeholder="Email" pattern="^[a-zA-Z0-9._%+\-]+@gmail\.com$" required id="register-email">
                    </div>
                    <div class="input-group">
                        <input type="password" placeholder="Password" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" required id="register-password">
                    </div>
                    <button type="submit" class="login-button">Register</button>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="/js/signin.js"></script>
</body>
</html>